

generator client {
  provider = "prisma-client"
  // output   = "../lib/generated/prisma"
}

datasource db {
  provider = "postgresql"
  // this is no longer supported edit in prisma.config.ts
  // url =  env("DATABASE_URL")
}
